export class JSearchAdapter {
  constructor() {
    this.name = 'JSearch';
  }

// eslint-disable-next-line no-unused-vars
  async fetchOpportunities(options = {}) {
    // TODO: Implement JSearch API integration
    console.log(`Fetching opportunities from ${this.name}...`);
    return [];
  }
}
