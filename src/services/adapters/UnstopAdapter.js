export class UnstopAdapter {
  constructor() {
    this.name = 'Unstop';
  }

// eslint-disable-next-line no-unused-vars
  async fetchOpportunities(options = {}) {
    // TODO: Implement Unstop integration
    console.log(`Fetching opportunities from ${this.name}...`);
    return [];
  }
}
